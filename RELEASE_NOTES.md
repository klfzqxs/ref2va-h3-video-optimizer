# Ref2VA H3 视频提示词优化器 · 版本说明

> 版本：**v1.1 正式版** · 协议：MIT · 联系：微博 @快乐肥宅庆先森

本说明按版本汇总**新增、改动与修复**；v1.0 的首发内容保留在下方作为基线。

---

## 一、v1.1 更新

### 1. 新增 I2VA 流程（首帧图生视频）
- 参考图区上方新增**流程选择**：`Ref2VA`（参考图/视频 → 视频）或 `I2VA`（首帧图生视频）。
- **I2VA 约束**：只能选 **1 张**参考图，并**强制作为视频第一帧**；不吃参考音频；模式 A/B 自动隐藏。
- **提示词按官方 I2VA 规范写英文**（首帧声明 + `integrated_multimodal_description` / `overall_soundscape` / `non_diegetic_music` 三字段）；**剧本仍为中文**，仅台词与画面内文字按规范逐字保留原文。
- 使用 `workflows/video_minimax_h3_i2v.api.json`，并接入 **SageAttention3** 两个节点（`PathchSageAttentionKJ` + `MiniMaxH3MemoryEfficientSageAttentionPatch`，与 Ref2VA 一致）——实测采样 **~30.2 → ~22.6 s/it（约 1.26×）**，采样峰值显存 **12.3 → 10.4 GB**。
- I2VA 的 **sampler / scheduler / steps 也可从页面设置**（不再锁定在模板值）。

### 2. 快速渲染机制重做
- **不再切换工作流、不再依赖任何 turbo LoRA**。快速渲染与精渲使用**同一个工作流**：模型、采样器、时长、画幅、LoRA **全部相同**，只把**分辨率（megapixels）与采样步数各乘 0.707 并向下取整**。
- 因 0.707 × 0.707 = 0.5，**渲染耗时约为精渲的一半**（实测 2.04×–2.55×；向下取整只会更快，不会更慢）。
- 两条流程（Ref2VA / I2VA）都支持；配置字段由 `quick_workflow` 更名为 **`quick_render`**。
- 加速 LoRA（lightx2v / 8 步 turbo 类等）**完全由你在页面「LoRA」区自行加载**——两条流程的工作流内均不含任何内置 LoRA。

### 3. Ref2VA 提示词生成加速（KV 前缀缓存）
- 官方写作指南（约 6k token）从 user 消息中段**移入 system prompt**，变化的剧本 JSON 移到 user 末尾。
- 这样同一轮优化里的多次调用共享同一个稳定前缀，服务端（llama.cpp 等）的**前缀复用可整段跳过指南的 prefill**——实测单次转译 **14.6 s → 2.0 s**（省约 12.6 s/次）。
- CLI 路径（`core/ref2va_auto.py`）的同类写法一并统一。

### 4. 参考编号统一为从 1 开始
- 给 LLM 的参考清单此前是 **0 起**编号，而 H3 的引用标签 `<Picture N>` / `<Audio N>` 从 **1** 起（第 1 张参考图接的正是 `ref_image_0` 槽位 → 呈现为 `<Picture 1>`），两者语义冲突。
- 现在清单**直接标出标签**（`<Picture 1> = xxx.png · 参考：山峰`），并在导演 / 格式师 system prompt 中明确**禁止 0 号、禁止重新编号、禁止整体平移**。
- 避免"最终提示词被规范纠正、但中间产物里 0/1 混用"继续向下游（与爬山回炉）传播。

---

## 二、v1.1 修复的问题

| # | 修复内容 |
|---|---------|
| 1 | 修复了 I2VA 下 sampler / scheduler / steps 无法生效（被锁在模板值）的问题 |
| 2 | 修复了 I2VA 工作流未启用 SageAttention3、渲染显著偏慢的问题 |
| 3 | 修复了参考图像编号从 0 起、与 H3 的 `<Picture 1>` 不一致的问题 |
| 4 | 修复了快速渲染依赖特定 turbo LoRA、缺失时会静默回退的问题 |
| 5 | 修复了 I2VA 下「快速渲染」选项不可用的问题 |

---

## 三、v1.0 基线能力（首发）

### 1. 图形化 Web 控制台（新增）
- 纯标准库后端（`server.py`）＋ 中文网页（`web/index.html`），浏览器操作，无需命令行。
- **一键启动**：双击 `run.bat`，自动选 Python、起服务、**自动打开浏览器**。
- 地址：本机 `http://127.0.0.1:8090/`，局域网 `http://<本机IP>:8090/`。

### 2. 两种参考模式
- **A · 仅参考图/文字**：省显存、效果略差。
- **B · 视频编辑**：显存消耗大、效果最佳；**自动把上一步生成的视频作为下一步参考**（无需手动传视频）。

### 3. 参考素材逐个注明“参考什么”
- 每张参考图、每条参考音频可填一句话用途（如：山峰/森林/瀑布/天空/光影；流水声/鸟鸣/风声）。
- **音频参考**：有参考音频且配置音频 LLM 时，自动把“用户填的用途 + 音频 LLM 听声分析出的声音特征”一起写入提示词，供写剧本/转译还原。

### 4. 纯文字驱动 T2VA
- 参考图可全部留空＝纯文字描述生成视频。

### 5. LLM 模型一键读取
- 填好 LLM 地址 + API Key 后点「读取 LLM 模型列表」，从该端点 `/v1/models` 读取可用模型 id，填入模型下拉。

### 6. ComfyUI 模型一键读取 + 模型名自愈
- 自动列出该 ComfyUI 的 **UNET / CLIP / VAE / LoRA / 采样器 / 调度器**，下拉/可搜选择。
- **模型名规范化**：提交前扫描整张图，把 `lora_name/unet_name/clip_name/vae_name/ckpt_name` 按目标 ComfyUI 列表自动修正（含子目录前缀自愈、裸名也能对上）。

### 7. 任务排队 + 历史回看
- 任务运行时不禁止「运行优化」，而是变「**任务排队**」，点击新增队列，**串行依次执行**。
- 「历史」栏每个任务可点击**轮询日志**，并**回填该任务的预填写信息**（含参考素材备注）。

### 8. 强制终止当前任务
- 终止运行/排队任务，并**定向取消它提交到 ComfyUI 的该任务作业**（删除仍在排队的、中断属于该任务的运行项），**不影响 ComfyUI 里其它排队任务**。

### 9. 日志分阶段 + 时间戳 + 耗时
- 写剧本 / 转译 / 渲染 / 评分每阶段打印 `[HH:MM:SS] ▸/◂ 开始·结束（耗时 Xs）`。

### 10. 日志自动 / 手动刷新
- 日志旁加「自动刷新」（默认开）+「刷新」按钮。

### 11. LoRA 加载器
- 渲染参数里可**加多条 LoRA**，每条选模型名＋设强度(0~2)；留空=不加。

---

## 四、v1.0 修复的问题

| # | 修复内容 |
|---|---------|
| 1 | 修复了工作流里不选择LoRA时会报错的问题 |
| 2 | 修复了模型名/CLIP 用中性占位名导致 ComfyUI 报 `not in list` 的问题 |
| 3 | 修复了纯文字(T2VA)模式残留占位 LoadImage/LoadAudio 导致 `Invalid image file` 的问题 |
| 4 | 修复了提示词时长写死 15 秒的问题 |
| 5 | 修复了缺 ffmpeg 时无限重写提示词的问题 |
| 6 | 修复了日志只显示 `[server] starting` 不滚动的问题 |
| 7 | 修复了使用部分 AMD 显卡和 ROCm 版本 ComfyUI 时会偶发出现掉驱动的问题 |
| 8 | 修复了 B 路径（视频修改）优化模式无法正常使用的问题 |
| 9 | 修复了连发任务时 `/api/jobs` 卡死的问题 |
| 10 | 修复了刷新页面会丢失预填信息的问题 |
| 11 | 修复了 `AttributeError`报错 的问题 |
| 12 | 修复了提交后日志不再自动滚动的问题 |
| 13 | 修复了画幅下拉用了非 ComfyUI 值导致 `not in list` 的问题 |
| 14 | 修复了采样器/调度器下拉选项太多不好点选的问题 |
| 15 | 修复了读取模型连不上或读到旧地址的问题 |
| 16 | 修复了转译被 500 字限制、复杂剧本锚定不精确的问题 |
| 17 | 修复了剧本缺失（空剧本）仍继续优化的问题 |
| 18 | 修复了参考音频功能不生效（缺少听声分析）的问题 |

---

## 五、性能 / 体验优化（累计）

- **v1.1**：快速渲染改为"同工作流、仅缩分辨率与步数"，耗时约减半（实测 2.04×–2.55×）。
- **v1.1**：I2VA 接入 SageAttention3，采样提速约 1.26×、峰值显存降约 1.9 GB。
- **v1.1**：Ref2VA 把官方指南移入 system prompt，转译阶段省掉整段指南的重复 prefill（约 12.6 s/次，且候选越多越省）。
- **v1.1**：参考编号与 H3 标签统一为 1 起，消除隐蔽的错位风险。
- 转译不限字数、逐拍详转 → 复杂剧本提示词锚定更精确。
- 采样器/调度器、模型/CLIP/LoRA 从 ComfyUI 读取 → 避免手填写错导致的校验失败，配置更稳。
- 分阶段日志带时间戳/耗时 → 便于定位慢点。
- 读取模型支持指定 ComfyUI 地址 → 适配不同端口/子目录放置。

---

## 六、已知限制

- **上传的参考文件**（图/音频）因浏览器安全限制，刷新或切换任务后需重新上传；参数、备注、模式会自动带出。
- **B 方式**显存消耗更高；依赖本机 ComfyUI 在线（默认 `http://127.0.0.1:8000`）。
- **快速渲染**只缩分辨率与步数（各 ×0.707），**不会替你加载任何加速 LoRA**；想更快请在「LoRA」区自行加载（如 lightx2v / 8 步 turbo 类），并把步数设成与该 LoRA 匹配的值。
- **I2VA** 需 ComfyUI 装有 `MiniMaxH3ImageToVideo` / `MiniMaxH3SigmaShift` / `ResolutionSelector` / `ComfyMathExpression`，以及 `ComfyUI-KJNodes` 提供的 `PathchSageAttentionKJ` 与 `MiniMaxH3MemoryEfficientSageAttentionPatch`（需 sageattention 3）；两条流程都需要 `ComfyUI-KJNodes` 做 SageAttention 加速。
- 模型文件、ffmpeg、LLM/音频 LLM 端点需自行准备。

---

## 七、怎么用（一句话）

解压 → 双击 `run.bat` → 打开 `http://127.0.0.1:8090/` → 点「读取 ComfyUI 模型列表」选好模型/CLIP/LoRA → 选流程（Ref2VA / I2VA）→ 填画面描述与参考素材 → 按需勾「快速渲染」→ 点「运行优化」/「任务排队」。

> 技术细节见 `README.md`；给使用者的简短说明见 `使用说明.md`。
