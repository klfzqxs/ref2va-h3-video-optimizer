# Ref2VA H3 视频提示词优化器 · 版本说明

> 版本：**v1.0 正式版** · 协议：MIT · 更新日期：当前 · 联系：微博 @快乐肥宅庆先森

本说明汇总 v1.0 相比最初版本**新增了什么、修复了什么**。

---

## 一、新增能力

### 1. 图形化 Web 控制台（新增）
- 纯标准库后端（`server.py`）＋ 中文网页（`web/index.html`），浏览器操作，无需命令行。
- **一键启动**：双击 `run.bat`，自动选 Python、起服务、**自动打开浏览器**。
- 地址：本机 `http://127.0.0.1:8090/`，局域网 `http://<本机IP>:8090/`。

### 2. 两种参考模式
- **A · 仅参考图/文字**：省显存、效果略差。
- **B · 视频编辑**：显存消耗大、效果最佳；**自动把上一步生成的视频作为下一步参考**（无需手动传视频）。

### 3. 参考素材逐个注明“参考什么”
- 每张参考图、每条参考音频可填一句话用途（如：山峰/森林/瀑布/天空/光影；流水声/鸟鸣/风声）。
- **音频参考**：有参考音频且配置音频 LLM 时，自动把“用户填的用途 + 音频 LLM 听声分析出的声音特征”一起写入提示词，供写剧本/转译还原。

### 4. 纯文字驱动 T2VA
- 参考图可全部留空＝纯文字描述生成视频。

### 5. LLM 模型一键读取
- 填好 LLM 地址 + API Key 后点「读取 LLM 模型列表」，从该端点 `/v1/models` 读取可用模型 id，填入模型下拉。

### 6. ComfyUI 模型一键读取 + 模型名自愈
- 自动列出该 ComfyUI 的 **UNET / CLIP / VAE / LoRA / 采样器 / 调度器**，下拉/可搜选择。
- **模型名规范化**：提交前扫描整张图，把 `lora_name/unet_name/clip_name/vae_name/ckpt_name` 按目标 ComfyUI 列表自动修正（含子目录前缀自愈、裸名也能对上）。

### 7. 任务排队 + 历史回看
- 任务运行时不禁止「运行优化」，而是变「**任务排队**」，点击新增队列，**串行依次执行**。
- 「历史」栏每个任务可点击**轮询日志**，并**回填该任务的预填写信息**（含参考素材备注）。

### 8. 强制终止当前任务
- 终止运行/排队任务，并**定向取消它提交到 ComfyUI 的该任务作业**（删除仍在排队的、中断属于该任务的运行项），**不影响 ComfyUI 里其它排队任务**。

### 9. 日志分阶段 + 时间戳 + 耗时
- 写剧本 / 转译 / 渲染 / 评分每阶段打印 `[HH:MM:SS] ▸/◂ 开始·结束（耗时 Xs）`。

### 10. 日志自动 / 手动刷新
- 日志旁加「自动刷新」（默认开）+「刷新」按钮。

### 11. LoRA 加载器
- 渲染参数里可**加多条 LoRA**，每条选模型名＋设强度(0~2)，**精渲与快速模式都生效**；留空=不加。

### 12. 关闭源发布
- 4 个 `.py` 已转义（源码不出现中文），打包为 zip 即用。

---

## 二、修复的问题

| # | 修复内容 |
|---|---------|
| 1 | 修复了工作流里残留的一个风格化 LoRA 的问题 |
| 2 | 修复了模型名/CLIP 用中性占位名导致 ComfyUI 报 `not in list` 的问题 |
| 3 | 修复了纯文字(T2VA)模式残留占位 LoadImage/LoadAudio 导致 `Invalid image file` 的问题 |
| 4 | 修复了提示词时长写死 15 秒的问题 |
| 5 | 修复了缺 ffmpeg 时无限重写提示词的问题 |
| 6 | 修复了日志只显示 `[server] starting` 不滚动的问题 |
| 7 | 修复了使用部分 AMD 显卡和 ROCm 版本 ComfyUI 时会偶发出现掉驱动的问题 |
| 8 | 修复了 B 路径（视频修改）优化模式无法正常使用的问题 |
| 9 | 修复了连发任务时 `/api/jobs` 卡死的问题 |
| 10 | 修复了刷新页面会丢失预填信息的问题 |
| 11 | 修复了一跑真任务就崩 `AttributeError` 的问题 |
| 12 | 修复了提交后日志不再自动滚动的问题 |
| 13 | 修复了画幅下拉用了非 ComfyUI 值导致 `not in list` 的问题 |
| 14 | 修复了采样器/调度器下拉选项太多不好点选的问题 |
| 15 | 修复了读取模型连不上或读到旧地址的问题 |
| 16 | 修复了转译被 500 字限制、复杂剧本锚定不精确的问题 |
| 17 | 修复了剧本缺失（空剧本）仍继续优化的问题 |
| 18 | 修复了参考音频功能不生效（缺少听声分析）的问题 |

---

## 三、性能 / 体验优化

- 转译不限字数、逐拍详转 → 复杂剧本提示词锚定更精确。
- 采样器/调度器、模型/CLIP/LoRA 从 ComfyUI 读取 → 避免手填写错导致的校验失败，配置更稳。
- 分阶段日志带时间戳/耗时 → 便于定位慢点。
- 读取模型支持指定 ComfyUI 地址 → 适配不同端口/子目录放置。

---

## 四、已知限制

- **上传的参考文件**（图/音频）因浏览器安全限制，刷新或切换任务后需重新上传；参数、备注、模式会自动带出。
- **B 方式**显存消耗更高；依赖本机 ComfyUI 在线（默认 `http://127.0.0.1:8000`）。
- **快速工作流（turbo 8 步）** 需 `ComfyUI\models\lora\` 下有 `minimax_h3_ref2v_turbo_8step_v1.0_768p_comfyui_resized_avg_rank_64_bf16.safetensors`；缺失自动回退标准工作流。
- 模型文件、ffmpeg、LLM/音频 LLM 端点需自行准备。

---

## 五、怎么用（一句话）

解压 → 双击 `run.bat` → 打开 `http://127.0.0.1:8090/` → 点「读取 ComfyUI 模型列表」选好模型/CLIP/LoRA → 填画面描述与参考素材 → 选 A/B → 点「运行优化」/「任务排队」。

> 技术细节见 `README.md`；给使用者的简短说明见 `使用说明.md`。
