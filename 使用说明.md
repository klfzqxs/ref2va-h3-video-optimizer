# Ref2VA H3 优化器 · 使用说明

欢迎使用！这是一个「MiniMAX H3 视频提示词优化器」，用来把一段视频的提示词迭代优化到更好画质/表现。支持 **Ref2VA**（参考图/视频 → 视频）与 **I2VA**（首帧图生视频）两条流程。

> 技术文档见 `README.md`；这里只讲怎么跑起来、填什么。

## 一、启动

双击 `run.bat`，它会自动用 Python 启动服务并打开浏览器：

- 本机访问：`http://127.0.0.1:8090/`
- 局域网（其他电脑）：`http://<运行这台电脑的IP>:8090/`

- 如果双击后没反应，或提示找不到 `python`：用记事本打开 `run.bat`，在最上面加一行
  `set "PYTHON=C:\Python\python.exe"`（换成你的 Python 路径），保存再双击。
- 停止：在黑色窗口按 `Ctrl+C`（或直接关窗）。

## 二、前置依赖（在这台跑 `run.bat` 的机器上）

- 一个正在运行的 **ComfyUI**（页面里默认 `http://127.0.0.1:8000`），已装好你在用的 MiniMax-H3 UNET/CLIP/VAE/LoRA。
- 一个**写剧 + 评分**用的 LLM 端点（OpenAI 兼容、能看图多模态）；用云端 API 也可以。
- 系统里能直接调用的 **ffmpeg**（用于抽帧评分）。
- 快速工作流（turbo 8 步）需要 turbo LoRA；没装会自动回退标准工作流，不影响使用。

## 三、页面上填什么

| 区块 | 说明 |
|------|------|
| **流程**（参考图区上方） | **Ref2VA**·参考图/视频 → 视频（六段英文提示词，支持多图与 A/B 模式）；**I2VA**·首帧图生视频（**只能选 1 张**图、强制作为视频第一帧，按官方 I2VA 规范写**英文**提示词） |
| 画面内容描述 | 一句话说清这段视频要拍什么（纯文字即可；不参考图时=纯文字驱动 T2VA） |
| 参考图 / 参考音频（可选） | 点「＋添加」，上传后在后面对话框注明「参考什么」（如山峰/森林/瀑布/天空/光影；流水声/鸟鸣/风声）。可全部留空。**I2VA 下只能 1 张图，且参考音频区会自动隐藏** |
| 参考模式（仅 Ref2VA） | **A**·仅参考图/文字（省显存、效果略差）；**B**·视频编辑（显存消耗大、效果最佳；自动用上一步生成的视频，无需传视频） |
| 模型 / 采样参数 | 模型文件名（diffusion_models 内）、采样器/调度器/步数、Seed、分辨率、画幅、时长 |
| 快速工作流（仅 Ref2VA） | 勾选=用 turbo 8 步提速；未装 turbo LoRA 会自动回退标准。**I2VA 无此选项**——I2V 工作流不含内置 LoRA，要加速请在下方「LoRA」区自行添加 |
| LoRA | 两条流程通用，可加多条、各设强度。**I2VA 也在这里加**（工作流内不含内置 LoRA），例如加速用的 lightx2v / motion enhancer 类 |
| 结束精渲 | 勾选=爬山结束用全质量工作流再精渲一次最终产物（略慢，效果更稳） |
| LLM 端点 | 写剧/评分模型地址 + 模型 id；用云端 API 时再填 API Key |
| 音频评审 | **默认关闭**（在线音频模型少、本地部署麻烦）；确需开启才填音频 LLM 地址/模型/Key 并勾选 |

## 四、结果

- 页面下方**运行日志**实时刷新。
- 优化结束（达标 / 迭代上限 / 刷不动）后，**结果视频 / 最优 Prompt** 区会显示当前最优那条视频与它的完整提示词，可直接观看/下载。

## 五、常见问题

- **启动后浏览器没开**：手动访问 `http://127.0.0.1:8090/`。
- **报「llm_base / llm_model 必填」**：页面上把 LLM 地址 + 模型 id 填好（模型 id 用端点 `/v1/models` 查）。
- **参考图报文件不存在**：确认用「＋添加参考图」上传过；路径由服务端自动处理。
- **渲染失败/无输出**：先确认 ComfyUI 在 `comfy_url` 上运行、模型文件已放好；可看日志里的具体报错。
- **快速工作流提示「未检测到 turbo LoRA」**：执行时自动回退标准工作流，属正常。
- **选 I2VA 后提示「必须提供 1 张参考图」**：I2VA 必须上传 1 张图，它会强制作为视频第一帧。
- **选 I2VA 后「模式 A/B」「参考音频」「快速工作流」消失了**：正常——I2V 工作流只吃首帧图、不吃参考音频，且本身即 turbo。

## 六、联系

任何使用问题请微博联系 **@快乐肥宅庆先森**。

> 注意：这是一台单条剪辑的优化器，不是长视频生成器；B 方式会消耗较多显存，请按你的显卡配置选择 A/B。
